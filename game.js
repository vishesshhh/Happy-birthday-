const SECRET='HAPPY BIRTHDAY UPMA';const rows=['QWERTYUIOP','ASDFGHJKL','ZXCVBNM'];const slots=document.getElementById('slots'),keyboard=document.getElementById('keyboard'),progress=document.getElementById('progress'),hint=document.getElementById('hint');let index=0;const letters=[...SECRET].filter(c=>/[A-Z]/.test(c));
for(const c of SECRET){const s=document.createElement('span');if(c===' '){s.className='slot';s.style.border='0';s.style.width='clamp(12px,3vw,25px)'}else{s.className='slot';s.textContent='•';s.dataset.letterIndex=index++;}slots.appendChild(s)}
rows.forEach(row=>{const r=document.createElement('div');r.className='key-row';[...row].forEach(ch=>{const b=document.createElement('button');b.className='key';b.type='button';b.textContent=ch;b.dataset.letter=ch;b.onclick=()=>tap(b);r.appendChild(b)});keyboard.appendChild(r)});index=0;function target(){document.querySelectorAll('.key').forEach(k=>k.classList.toggle('target',k.dataset.letter===letters[index]));}function tap(b){if(index>=letters.length)return;if(b.dataset.letter!==letters[index]){b.animate([{transform:'translateX(0)'},{transform:'translateX(-7px)'},{transform:'translateX(7px)'},{transform:'translateX(0)'}],{duration:240});return}const slot=[...document.querySelectorAll('.slot')].find(s=>s.dataset.letterIndex==index);if(slot){slot.textContent=letters[index];slot.classList.add('revealed')}index++;progress.style.width=(index/letters.length*100)+'%';target();if(index===letters.length){hint.textContent='You found it ❤️';document.querySelectorAll('.key').forEach(k=>k.classList.remove('target'));setTimeout(()=>{slots.animate([{opacity:1},{opacity:0}],{duration:550,fill:'forwards'});hint.animate([{opacity:1},{opacity:0}],{duration:450,fill:'forwards'});burst(180);setTimeout(()=>location.href='photo.html',700)},350)}}target();
const canvas=document.getElementById('confettiCanvas'),ctx=canvas.getContext('2d');let ps=[],running=false;function size(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(d,0,0,d,0,0)}addEventListener('resize',size);size();function burst(n){for(let i=0;i<n;i++){let a=Math.random()*Math.PI*2,s=6+Math.random()*13;ps.push({x:innerWidth/2,y:innerHeight*.5,vx:Math.cos(a)*s,vy:Math.sin(a)*s-6,g:.2+Math.random()*.13,w:4+Math.random()*7,h:8+Math.random()*12,r:Math.random()*6,rs:(Math.random()-.5)*.3,life:1,c:.005+Math.random()*.006,col:['#ff4f87','#ffb45c','#fff','#b889ff','#70d8ff'][Math.floor(Math.random()*5)]})}if(!running){running=true;requestAnimationFrame(draw)}}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(let i=ps.length-1;i>=0;i--){let p=ps[i];p.vy+=p.g;p.x+=p.vx;p.y+=p.vy;p.r+=p.rs;p.life-=p.c;ctx.save();ctx.globalAlpha=Math.max(0,p.life);ctx.translate(p.x,p.y);ctx.rotate(p.r);ctx.fillStyle=p.col;ctx.fillRect(-p.w/2,-p.h/2,p.w,p.h);ctx.restore();if(p.life<=0||p.y>innerHeight+100)ps.splice(i,1)}if(ps.length){requestAnimationFrame(draw)}else running=false}
