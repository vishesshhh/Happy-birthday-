const c=document.getElementById('confettiCanvas');if(c){const x=c.getContext('2d');let p=[],run=0;function sz(){const d=Math.min(devicePixelRatio||1,2);c.width=innerWidth*d;c.height=innerHeight*d;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(d,0,0,d,0,0)}sz();addEventListener('resize',sz);window.burst=(n=220)=>{for(let i=0;i<n;i++){let a=Math.random()*6.283,s=7+Math.random()*14;p.push({x:innerWidth/2,y:innerHeight*.48,vx:Math.cos(a)*s,vy:Math.sin(a)*s-7,g:.23,w:5+Math.random()*8,h:9+Math.random()*14,r:Math.random()*6,rs:(Math.random()-.5)*.35,l:1,d:.006+Math.random()*.006,col:['#ff4f87','#ffb45c','#fff','#b889ff','#70d8ff'][Math.floor(Math.random()*5)]})}if(!run){run=1;requestAnimationFrame(draw)}}function draw(){x.clearRect(0,0,innerWidth,innerHeight);for(let i=p.length-1;i>=0;i--){let q=p[i];q.vy+=q.g;q.x+=q.vx;q.y+=q.vy;q.r+=q.rs;q.l-=q.d;x.save();x.globalAlpha=Math.max(0,q.l);x.translate(q.x,q.y);x.rotate(q.r);x.fillStyle=q.col;x.fillRect(-q.w/2,-q.h/2,q.w,q.h);x.restore();if(q.l<=0||q.y>innerHeight+80)p.splice(i,1)}if(p.length)requestAnimationFrame(draw);else run=0}}
if(document.getElementById('celebrate')){document.getElementById('celebrate').addEventListener('click',()=>{burst(420);document.querySelector('.celebrate-hint').textContent='Again! 🎉 Tap it again!'})}burst(70);
